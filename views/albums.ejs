<ul>
  <% albumsArray.forEach(album => { %>

  <h2><%= album.name %></h2>
  <img
    src="<%= album.images[0] ? album.images[0].url : '' %>"
    alt="<%= album.name %>"
  />
  <a href="/tracks/<%= album.id %>">View Tracks</a>

  <% }); %>
</ul>
